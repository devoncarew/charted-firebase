name: charted_firebase
version: 0.0.1
description: An absolute bare-bones web app.

environment:
  sdk: '>=1.0.0 <2.0.0'

dependencies:
  browser: '>=0.10.0 <0.11.0'
  charted: ^0.3.0
  # chartjs:  ^0.2.0
  dart_to_js_script_rewriter: '^1.0.1'
  firebase: ^0.6.6

transformers:
- dart_to_js_script_rewriter
